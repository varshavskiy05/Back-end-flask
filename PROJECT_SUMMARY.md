# 📊 Підсумок проєкту — Лабораторна робота №2 (Варіант 0)

## 🎯 Мета

Реалізувати облік доходів у фінансовому застосунку:
- кожен користувач має власний рахунок;
- доходи автоматично поповнюють баланс рахунку;
- витрати зменшують баланс;
- від’ємний баланс заборонений (захист від мінусових значень).

## 🧱 Стек та архітектура

- **Flask 3 + Flask-Smorest** — REST API з OpenAPI/Swagger документацією.
- **Flask-SQLAlchemy** — ORM-шар поверх PostgreSQL.
- **Flask-Migrate** — версіонування схеми БД.
- **Marshmallow** — валідація та серіалізація даних.
- **Docker Compose** — підйом PostgreSQL та застосунку.

## 🗃️ ORM-моделі

1. `User` — користувачі.
2. `Category` — глобальні категорії витрат.
3. `Account` — рахунок користувача (унікальний, містить баланс).
4. `Income` — записи доходів.
5. `Record` — записи витрат.

## 🔌 Основні ендпоінти

| Розділ | Маршрути |
|--------|---------|
| **Users** | `GET/POST /api/user`, `GET/PATCH/DELETE /api/user/<id>` |
| **Categories** | `GET/POST /api/category`, `GET/PATCH/DELETE /api/category/<id>` |
| **Accounts** | `GET/POST /api/account`, `GET /api/account/<id>`, `GET /api/account/user/<user_id>`, `PATCH/DELETE /api/account/<id>` |
| **Incomes** | `GET/POST /api/income`, `GET /api/income/<id>`, `DELETE /api/income/<id>` |
| **Records** | `GET/POST /api/record`, `GET /api/record/<id>`, `PATCH/DELETE /api/record/<id>` |
| **Health** | `GET /health` |

## ✅ Ключові можливості

- Валідація даних (Marshmallow) для всіх CRUD операцій.
- Баланс рахунку перераховується автоматично при доходах і витратах.
- Неможливість створити витрату, якщо баланс < суми списання.
- Уніфікована обробка помилок (400, 404, 422, 500).
- Початкові дані (`seed_data.py`) створюють тестового користувача, рахунок та приклади транзакцій.

## 📦 Документування та допоміжні файли

| Файл | Призначення |
|------|-------------|
| `README.md` | огляд застосунку, інсталяція, API |
| `SETUP_GUIDE.md` | покрокове налаштування середовища |
| `QUICKSTART.md` | запуск за 5 хвилин |
| `TESTING_SCENARIOS.md` | детальні сценарії тестування |
| `LAB_CHECKLIST.md` | контрольний список вимог |
| `PROJECT_SUMMARY.md` | (цей файл) підсумок реалізації |
| `Expenses_API.postman_collection.json` | Postman-колекція |
| `Expenses_API.postman_environment.json` | Postman environment |
| `test_api.sh` | bash-скрипт швидкого тестування |

## 🧪 Тестування

- Swagger UI: `http://localhost:8080/swagger-ui`.
- Postman: імпорт колекції + environment.
- Скрипт `./test_api.sh` виконує послідовність ключових запитів.
- `TESTING_SCENARIOS.md` містить сценарії для ручного тестування (включно з негативними кейсами).

## 🛠️ Налаштування

1. `pip install -r requirements.txt`
2. `docker-compose up db -d`
3. `./init_db.sh`
4. `flask run --host=0.0.0.0 --port=8080`

## 📈 Відповідність вимогам

| Критерій | Статус |
|----------|--------|
| Валідація та обробка помилок | ✅ |
| Робота з ORM та PostgreSQL | ✅ |
| Реалізація варіанту 0 (рахунок + доходи) | ✅ |
| Postman колекція + environment | ✅ |
| Коректний Git workflow | ✅ |

## 🧾 Підсумок

Застосунок повністю реалізує **варіант 0** лабораторної роботи №2: облік доходів із автоматичним оновленням рахунку та контролем балансу. Документація, скрипти та тести оновлені відповідно до обмеженого функціоналу, що значно спрощує перевірку та демонстрацію.

