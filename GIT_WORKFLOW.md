# Git Workflow для Лабораторної №2 (Варіант 0)

## 📝 Коміти та теги

### Крок 1: Перевірте статус
```bash
git status
```

### Крок 2: Додайте всі необхідні файли
```bash
git add .
# або вибірково
git add myapp/
git add requirements.txt
git add docker-compose.yaml
git add README.md
```

### Крок 3: Коміт із зрозумілим повідомленням
```bash
git commit -m "Lab 2: Variant 0 income tracking"
```

> Рекомендація: у описі коміту коротко згадайте про основні зміни (моделі, ендпоінти, документацію).

### Крок 4: Створіть тег версії
```bash
git tag v2.0.0 -a -m "Lab 2"
```

### Крок 5: Запуште коміти та тег
```bash
git push origin main
git push origin v2.0.0
# або одним рядком
git push origin main --tags
```

## 📋 Контрольний список перед пушем

- [ ] Всі потрібні файли додано до git.
- [ ] `.env` не відслідковується (перевірити `.gitignore`).
- [ ] Міграції створені та працюють (`flask db upgrade`).
- [ ] Тести пройдені (`./test_api.sh` або Postman).
- [ ] Документація оновлена під варіант 0.
- [ ] Тег `v2.0.0` створений.

```bash
# Перевірити, що все готово
git status

# Список відслідковуваних файлів
git ls-files

# Останні коміти
git log --oneline | head -n 5

# Теги
git tag -n
```

## 🔄 Корисні команди для правок

```bash
# Скасувати останній коміт, залишивши зміни
git reset --soft HEAD~1

# Змінити повідомлення останнього коміту
git commit --amend -m "New message"

# Видалити тег (локально)
git tag -d v2.0.0

# Видалити тег на сервері
git push origin :refs/tags/v2.0.0

# Створити тег заново
git tag v2.0.0 -a -m "Lab 2"
```

## 📦 Приклад структури комітів (необов’язково)

```bash
# 1. Базова конфігурація
git commit -m "Setup DB config and dependencies"

# 2. ORM та міграції
git commit -m "Add SQLAlchemy models and migrations for variant 0"

# 3. API ендпоінти
git commit -m "Implement accounts, incomes and expenses endpoints"

# 4. Документація та скрипти
git commit -m "Update docs and testing scripts for variant 0"
```

## 🚀 Публікація релізу (опційно)

1. Перейдіть на сторінку GitHub → *Releases*.
2. Створіть реліз, оберіть тег `v2.0.0`.
3. Додайте короткий опис (що реалізовано: рахунок, доходи, витрати, баланс).
4. Опублікуйте (необов'язково для лабораторної).

### Приклад опису релізу
```markdown
## Lab 2: Variant 0 — Income Tracking
- SQLAlchemy моделі: User, Category, Account, Income, Record
- REST API з Flask-Smorest + Swagger UI
- Валідація даних (Marshmallow)
- Автоматичне оновлення балансу при доходах/витратах
- Docker Compose для PostgreSQL
```

## 🧭 Стиль повідомлень

Рекомендується формат:
```
<type>: <короткий опис>

Докладніше:
- Пункт 1
- Пункт 2
```

**Приклади type:** `feat`, `fix`, `docs`, `refactor`, `test`, `chore`.

## ✅ Фінальні кроки після пушу

```bash
# Переконатися, що все на GitHub
git status

# Запустити контрольний скрипт
./test_api.sh

# Перевірити README та Swagger у браузері
open http://localhost:8080/swagger-ui
```

Проєкт готовий до захисту! 🎉

